<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cash Payment Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f3f3f3;
            color: #333;
            margin: 0;
            padding: 0;
        }

        .details-content {
            background-color: #fff;
            padding: 2%;
            margin-top: 2%;
            margin-left: 32%;
            margin-right: 32%;
            margin-bottom: 2%;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h4 {
            color: #555;
        }

        table {
            width: 80%;
            margin: 20px auto;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: #666;
        }

        input[type="text"] {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            display: inline-block;
        }

        .button {
            padding: 10px 20px;
            background-color: #5a5b63;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none; /* para quitar la subrayado del enlace */
            display: inline-block; /* para que el enlace se comporte como un botón */
        }

        .button:hover {
            background-color: #686978;
        }
        
        footer {
            background-color: #333;
            color: #fff;
            padding: 10px;
            text-align: center;
            margin-top: 1%;
            bottom: 0;
        }

        footer p {
            margin: 0;
            font-size: 14px;
        }
    </style>
</head>
<body>

<%- include("../partials/head.ejs") %>

<div class="details-content">
    <div align="center">
        <h4>ID pay from CASH:</h4>
        <p><%= cashPayment._id %></p>
    </div>
    <div class="content">
        <table>
            <tr>
                <td>
                    <label><b>Pay reference:</b></label>
                    <input type="text" value="<%= cashPayment.cashreference %>" disabled>
                </td>
            </tr>
            <tr>
                <td>
                    <label><b>User email:</b></label>
                    <input type="text" value="<%= cashPayment.cashuserEmail %>" disabled>
                </td>
            </tr>
            <tr>
                <td>
                    <label><b>Collector cash:</b></label>
                    <input type="text" value="<%= cashPayment.cashcollectorCash %>" disabled>
                </td>
            </tr>
            <tr>
                <td>
                    <label><b>Amount pay:</b></label>
                    <input type="text" value="$ <%= cashPayment.cashamount %>" disabled>
                </td>
            </tr>
            <tr>
                <td>
                    <label><b>Cellphone:</b></label>
                    <input type="text" value="<%= cashPayment.cashcellphone %>" disabled>
                </td>
            </tr>
        </table>
    </div>
</div>
<div class="button-group" align="center">
    <button class="button print" type="button" onclick="window.print();return false;">VOUCHER</button>
    <br />
    <br />    
    <a href="/cashform" class="button">BACK</a>
</div>

<%- include("../partials/footer.ejs") %>

</body>
</html>
